name: CI

on: [push]

jobs:
  functional-tests:
    - name: PHPUnit tests
      uses: php-actions/phpunit@v2
      with:
        version: 9
        php_version: 7.4
        configuration: /phpunit.xml.dist
        memory_limit: 256M